{
  "name": "starfire",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@12.6.0",
  "scripts": {
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "check": "biome check .",
    "typecheck": "pnpm --filter @starfire/core exec tsc --noEmit && pnpm --filter desktop exec tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "rust:fmt": "cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "rust:fmt:check": "cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --check",
    "rust:clippy": "cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets --all-features -- -D warnings"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "lefthook": "^2.1.14",
    "vitest": "^5.0.1"
  }
}
